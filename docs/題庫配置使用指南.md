# 題庫配置使用指南

## 📋 功能說明

透過編輯配置檔案，可以動態控制哪些題庫在應用程式中顯示。這讓您可以：
- 快速切換不同的題庫（IPAS、JAVA 等）
- 不需要重新編譯應用程式
- 在網頁版直接編輯配置檔案即可生效

## 🚀 快速開始

### 1. 找到配置檔案

配置檔案位置：`assets/config/quiz-library-config.json`

### 2. 編輯配置

開啟檔案，修改 `enabled` 欄位：

```json
[
  {
    "testName": "IPAS_01",
    "enabled": true,   // true = 顯示，false = 隱藏
    "displayName": "IPAS AI應用規劃師初級",
    "displayOrder": 1
  },
  {
    "testName": "JAVA",
    "enabled": true,    // 改為 true 即可顯示 JAVA 題庫
    "displayName": "Java 程式設計認證",
    "displayOrder": 3
  }
]
```

### 3. 重新載入

- **網頁版**：重新整理頁面（F5 或 Ctrl+R）
- **行動裝置**：重新啟動應用程式

## 📝 使用範例

### 範例 1：只顯示 IPAS_01

```json
[
  {
    "testName": "IPAS_01",
    "enabled": true,
    "displayName": "IPAS AI應用規劃師初級",
    "displayOrder": 1
  },
  {
    "testName": "IPAS_02",
    "enabled": false,  // 隱藏
    "displayName": "IPAS AI應用規劃師中級",
    "displayOrder": 2
  },
  {
    "testName": "JAVA",
    "enabled": false,  // 隱藏
    "displayName": "Java 程式設計認證",
    "displayOrder": 3
  }
]
```

### 範例 2：只顯示 JAVA

```json
[
  {
    "testName": "IPAS_01",
    "enabled": false,  // 隱藏
    "displayName": "IPAS AI應用規劃師初級",
    "displayOrder": 1
  },
  {
    "testName": "IPAS_02",
    "enabled": false,  // 隱藏
    "displayName": "IPAS AI應用規劃師中級",
    "displayOrder": 2
  },
  {
    "testName": "JAVA",
    "enabled": true,   // 顯示
    "displayName": "Java 程式設計認證",
    "displayOrder": 1  // 調整順序為第一
  }
]
```

### 範例 3：顯示所有題庫

```json
[
  {
    "testName": "IPAS_01",
    "enabled": true,
    "displayName": "IPAS AI應用規劃師初級",
    "displayOrder": 1
  },
  {
    "testName": "IPAS_02",
    "enabled": true,
    "displayName": "IPAS AI應用規劃師中級",
    "displayOrder": 2
  },
  {
    "testName": "JAVA",
    "enabled": true,   // 全部設為 true
    "displayName": "Java 程式設計認證",
    "displayOrder": 3
  }
]
```

## 🔧 新增題庫

如果要新增新的題庫（例如：Python），需要：

1. **確保題庫資料存在**
   - 在 `assets/data/questions/` 下建立對應的資料夾結構
   - 例如：`assets/data/questions/PYTHON/BASIC/11401.json`

2. **更新配置檔案**
   ```json
   {
     "testName": "PYTHON",
     "enabled": true,
     "displayName": "Python 程式設計認證",
     "displayOrder": 4
   }
   ```

3. **更新名稱映射**
   - 編輯 `src/utils/nameMapper.ts`
   - 在 `DEFAULT_TEST_NAME_MAP` 中加入：
   ```typescript
   'PYTHON': 'Python 程式設計認證',
   ```

4. **執行索引更新**
   ```bash
   node scripts/updateQuestionIndex.js
   ```

## ⚠️ 注意事項

1. **testName 必須一致**
   - 配置檔案中的 `testName` 必須與 `questions.json` 中的 `testName` 完全一致
   - 大小寫敏感

2. **JSON 格式**
   - 確保 JSON 格式正確（可以使用 JSON 驗證工具檢查）
   - 注意逗號和括號

3. **快取機制**
   - 網頁版會快取配置 5 分鐘
   - 如果修改後沒有立即生效，等待 5 分鐘或清除瀏覽器快取

4. **預設配置**
   - 如果配置檔案載入失敗，會使用程式碼中的預設配置
   - 預設配置在 `src/services/QuizLibraryConfigService.ts` 中定義

## 🐛 故障排除

### 問題：修改配置後沒有生效

**解決方案：**
1. 檢查 JSON 格式是否正確
2. 清除瀏覽器快取（Ctrl+Shift+R）
3. 確認檔案路徑正確：`assets/config/quiz-library-config.json`
4. 檢查瀏覽器控制台是否有錯誤訊息

### 問題：題庫沒有顯示

**解決方案：**
1. 確認 `enabled` 設為 `true`
2. 確認 `testName` 與實際資料一致
3. 檢查 `questions.json` 中是否有對應的題庫資料
4. 執行 `node scripts/updateQuestionIndex.js` 更新索引

### 問題：配置檔案無法載入

**解決方案：**
1. 確認檔案存在於 `assets/config/` 目錄
2. 確認檔案名稱正確：`quiz-library-config.json`
3. 檢查檔案權限
4. 在網頁版檢查 Network 標籤，確認請求是否成功

## 📚 相關檔案

- 配置檔案：`assets/config/quiz-library-config.json`
- 配置服務：`src/services/QuizLibraryConfigService.ts`
- 名稱映射：`src/utils/nameMapper.ts`
- 題庫列表頁：`src/screens/TestNameListScreen.tsx`

## 💡 進階用法

### 自訂顯示順序

透過 `displayOrder` 控制題庫的顯示順序：

```json
{
  "testName": "JAVA",
  "enabled": true,
  "displayName": "Java 程式設計認證",
  "displayOrder": 1  // 數字越小越前面
}
```

### 動態切換題庫

如果您想要在不同時間顯示不同的題庫，可以：
1. 準備多個配置檔案（例如：`config-ipas.json`, `config-java.json`）
2. 根據需要替換 `quiz-library-config.json`
3. 重新載入應用程式

## 🎯 最佳實踐

1. **版本控制**
   - 將配置檔案加入版本控制
   - 使用 Git 追蹤配置變更

2. **備份配置**
   - 修改前先備份原始配置
   - 保留多個配置版本以便快速切換

3. **測試**
   - 修改配置後，先在網頁版測試
   - 確認所有題庫都能正常顯示

4. **文件化**
   - 記錄每個題庫的用途
   - 說明為什麼某個題庫被啟用或停用

