# 專案結構與路徑說明

## 📁 專案目錄結構

```
quiz/
├── assets/                          # 靜態資源目錄
│   ├── config/                      # 配置檔案目錄
│   │   └── quiz-library-config.json # 題庫配置檔案
│   ├── data/                        # 資料目錄
│   │   ├── questions.json           # 題目索引檔案（自動生成）
│   │   └── questions/               # 題目 JSON 檔案目錄
│   │       ├── IPAS_01/             # IPAS 初級題庫
│   │       │   ├── L11/             # 科目 L11
│   │       │   │   ├── 11401.json   # 期數 11401 的題目
│   │       │   │   └── ...
│   │       │   └── L12/             # 科目 L12
│   │       ├── IPAS_02/             # IPAS 中級題庫
│   │       ├── AZ900/               # AZ900 題庫
│   │       └── JAVA/                # Java 題庫
│   └── images/                      # 圖片資源目錄
│       ├── IPAS_01/
│       │   └── L11/
│       │       └── 11401/           # 期數 11401 的圖片
│       │           ├── 3Q1.png      # 題目圖片
│       │           └── ...
│       └── IPAS_02/
│           └── L23/
│               └── 11411/
├── src/                              # 原始碼目錄
│   ├── components/                  # React 元件
│   ├── contexts/                    # React Context
│   ├── screens/                     # 畫面元件
│   ├── services/                    # 服務層
│   │   ├── QuestionService.ts      # 題目服務
│   │   ├── questionFileMap.ts      # 題目檔案映射表（自動生成）
│   │   ├── imageFileMap.ts          # 圖片檔案映射表（自動生成）
│   │   └── QuizLibraryConfigService.ts # 題庫配置服務
│   ├── types/                       # TypeScript 類型定義
│   └── utils/                       # 工具函數
├── scripts/                          # 腳本目錄
│   ├── updateQuestionIndex.js       # 更新題目索引（重要）
│   ├── updateImageFileMap.js        # 更新圖片映射表
│   ├── validatePaths.js             # 驗證路徑連結
│   ├── validateJson.js              # 驗證 JSON 格式
│   └── convertExcelToJSON.js        # Excel 轉 JSON
├── docs/                             # 文檔目錄
├── App.tsx                           # 應用程式入口
├── package.json                     # 專案配置
└── README.md                         # 專案說明文件
```

## 🔗 路徑引用說明

### 1. 題目檔案路徑

#### 檔案結構
題目檔案採用三層資料夾結構：
- **第一層**：測驗名稱（如 `IPAS_01`、`AZ900`、`JAVA`）
- **第二層**：科目名稱（如 `L11`、`part1`、`Spring boot`）
- **第三層**：期數檔案（如 `11401.json`、`20251216.json`）

#### 路徑格式
- **檔案系統路徑**：`assets/data/questions/{testName}/{subject}/{series_no}.json`
- **映射表鍵值**：`questions/{testName}/{subject}/{series_no}.json`
- **require 路徑**：`../../assets/data/questions/{testName}/{subject}/{series_no}.json`（相對於 `src/services/questionFileMap.ts`）

#### 範例
```typescript
// 檔案系統路徑
assets/data/questions/IPAS_01/L11/11401.json

// 映射表鍵值（用於查找）
'questions/IPAS_01/L11/11401.json'

// require 路徑（在 questionFileMap.ts 中）
require('../../assets/data/questions/IPAS_01/L11/11401.json')
```

### 2. 圖片檔案路徑

#### 檔案結構
圖片檔案採用三層資料夾結構：
- **第一層**：測驗名稱（如 `IPAS_01`、`IPAS_02`）
- **第二層**：科目名稱（如 `L11`、`L23`）
- **第三層**：期數資料夾（如 `11401`、`11411`）
- **檔案**：圖片檔案（如 `3Q1.png`、`5A1.png`）

#### 路徑格式
- **檔案系統路徑**：`assets/images/{testName}/{subject}/{series_no}/{imageFile}`
- **映射表鍵值**：`{testName}/{subject}/{series_no}/{imageFile}`
- **require 路徑**：`../../assets/images/{testName}/{subject}/{series_no}/{imageFile}`（相對於 `src/services/imageFileMap.ts`）

#### 範例
```typescript
// 檔案系統路徑
assets/images/IPAS_01/L11/11401/3Q1.png

// 映射表鍵值（用於查找）
'IPAS_01/L11/11401/3Q1.png'

// require 路徑（在 imageFileMap.ts 中）
require('../../assets/images/IPAS_01/L11/11401/3Q1.png')
```

### 3. 配置檔案路徑

#### 檔案位置
- **檔案系統路徑**：`assets/config/quiz-library-config.json`
- **Web 載入路徑**：`/assets/config/quiz-library-config.json`
- **本地儲存鍵值**：`@quiz:libraryConfig`（AsyncStorage）

### 4. 索引檔案路徑

#### 檔案位置
- **檔案系統路徑**：`assets/data/questions.json`
- **require 路徑**：`../../assets/data/questions.json`（相對於 `src/services/QuestionService.ts`）
- **Web 載入路徑**：`/assets/data/questions.json`

## 🔧 路徑驗證

### 驗證腳本
使用 `scripts/validatePaths.js` 驗證所有路徑連結：

```bash
node scripts/validatePaths.js
```

### 驗證項目
1. ✅ 題目檔案路徑（questionFileMap.ts）
2. ✅ 圖片檔案路徑（imageFileMap.ts）
3. ✅ 索引檔案中的路徑引用
4. ✅ 配置檔案格式
5. ✅ 主要索引檔案結構
6. ✅ 應用程式資源（如 back.png）

### 驗證結果
所有路徑驗證通過後，會顯示：
```
✅ 所有路徑驗證通過！
```

## 📝 路徑更新流程

### 新增題目檔案時

1. **放置檔案**
   ```bash
   # 將 JSON 檔案放入對應資料夾
   assets/data/questions/{testName}/{subject}/{series_no}.json
   ```

2. **更新索引**
   ```bash
   node scripts/updateQuestionIndex.js
   ```
   此腳本會：
   - 掃描所有題目檔案
   - 更新 `assets/data/questions.json`
   - 更新 `src/services/questionFileMap.ts`

3. **驗證路徑**
   ```bash
   node scripts/validatePaths.js
   ```

### 新增圖片檔案時

1. **放置檔案**
   ```bash
   # 將圖片檔案放入對應資料夾
   assets/images/{testName}/{subject}/{series_no}/{imageFile}
   ```

2. **更新映射表**
   ```bash
   node scripts/updateImageFileMap.js
   ```
   此腳本會：
   - 掃描所有圖片檔案
   - 更新 `src/services/imageFileMap.ts`

3. **驗證路徑**
   ```bash
   node scripts/validatePaths.js
   ```

## ⚠️ 注意事項

### 1. 路徑一致性
- 確保檔案系統路徑與映射表鍵值一致
- 使用正斜線 `/` 作為路徑分隔符（即使在 Windows 上）
- 路徑大小寫敏感（Linux/macOS）

### 2. 自動生成檔案
以下檔案由腳本自動生成，**請勿手動編輯**：
- `src/services/questionFileMap.ts`
- `src/services/imageFileMap.ts`
- `assets/data/questions.json`

### 3. 路徑相對性
- `require()` 路徑是相對於**當前檔案**的位置
- `questionFileMap.ts` 位於 `src/services/`，所以使用 `../../assets/...`
- `imageFileMap.ts` 位於 `src/services/`，所以使用 `../../assets/...`

### 4. Web 平台路徑
在 Web 平台上，使用 `fetch()` 載入資源：
- 題目索引：`/assets/data/questions.json`
- 題目檔案：`/assets/data/{filePath}`
- 配置檔案：`/assets/config/quiz-library-config.json`

## 🔍 故障排除

### 問題：找不到檔案

**檢查項目：**
1. 確認檔案是否存在於正確位置
2. 檢查路徑大小寫是否正確
3. 確認資料夾結構是否符合規範
4. 執行 `node scripts/validatePaths.js` 驗證

### 問題：require 路徑錯誤

**解決方案：**
1. 確認 require 路徑是相對於當前檔案的位置
2. 檢查路徑分隔符是否正確（使用 `/`）
3. 重新執行對應的更新腳本

### 問題：映射表未更新

**解決方案：**
1. 執行 `node scripts/updateQuestionIndex.js`（題目）
2. 執行 `node scripts/updateImageFileMap.js`（圖片）
3. 重新啟動應用程式

## 📚 相關文檔

- [檔案功能說明](./檔案功能說明.md)
- [題庫配置使用指南](./題庫配置使用指南.md)
- [README.md](../README.md)

